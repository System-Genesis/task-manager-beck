interface btnInterface {
  title: String;
  params: Object;
  name: String;
  select: String;
  message: String;
  type: String;
  methods: String;
}

interface btnTitle {
  title: String;
}

export { btnInterface, btnTitle };
